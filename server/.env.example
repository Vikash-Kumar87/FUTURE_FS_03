PORT=4000
CLIENT_ORIGIN=http://localhost:5173
ADMIN_EMAIL=admin@futurefit.com
# Optional extra protection for admin endpoints.
# If set, frontend must send matching VITE_ADMIN_API_KEY value.
ADMIN_API_KEY=
# Optional: custom path for persisted JSON data file.
# Useful on cloud hosts that mount persistent disks.
DATA_FILE_PATH=
